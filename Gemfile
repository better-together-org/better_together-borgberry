source 'https://rubygems.org'

better_together_path = ENV.fetch(
  'BETTER_TOGETHER_PATH',
  File.expand_path('../community-engine-rails', __dir__)
)

eval_gemfile File.join(better_together_path, 'Gemfile')

gemspec
